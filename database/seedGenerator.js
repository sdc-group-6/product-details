module.exports = (recordCount, sql) => {
  let products = {};
  let looks = {};
  let shares = {};
  
  // random number helper
  
  // determine number of each type of product to generate

  return { products, looks, shares };
};